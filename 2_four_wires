Solution:

module top_module( 
    input a,b,c,
    output w,x,y,z );
    
    assign w = a;
    assign x = b;
    assign y = b;
    assign z = c;

endmodule

Note:
  The order in which they appear in the code does not matter. (parallel)
